1. learning_openai is the main learning file based on OpenAI package
	a. It uses tracked_env package
2. Custom algorithms are located in algorithms 
	a. actors and critics are correspondant neural networks
3. learning file is used for custom learning with REPS, SARSA and REINFORCE, it depends on custom_env. These files should be deleted in the future.
4. libs located in the parent folder is the packages where useful sub-packages are presented. The sub-package stability is necessary for stability assesment
